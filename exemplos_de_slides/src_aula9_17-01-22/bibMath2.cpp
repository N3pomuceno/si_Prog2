#include<math.h>

int main() {
    double angulo = 1.04716; // 60 em radiano
    double cosseno, seno, tangente;
    cosseno = cos(angulo);
    seno = sin(angulo);
    tangente = tan(angulo);
    return 0;
}